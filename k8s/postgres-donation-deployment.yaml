apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -o k8s/
    kompose.version: 1.38.0 (HEAD)
  labels:
    io.kompose.service: postgres-donation
  name: postgres-donation
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: postgres-donation
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -o k8s/
        kompose.version: 1.38.0 (HEAD)
      labels:
        io.kompose.service: postgres-donation
    spec:
      containers:
        - env:
            - name: POSTGRES_DB
              value: glense_donation
            - name: POSTGRES_PASSWORD
              value: changeme
            - name: POSTGRES_USER
              value: glense
          image: postgres:16-alpine
          livenessProbe:
            exec:
              command:
                - pg_isready -U glense -d glense_donation
            failureThreshold: 5
            periodSeconds: 10
            timeoutSeconds: 5
          name: glense-postgres-donation
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgres-donation-data
      restartPolicy: Always
      volumes:
        - name: postgres-donation-data
          persistentVolumeClaim:
            claimName: postgres-donation-data
