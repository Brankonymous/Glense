apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -o k8s/
    kompose.version: 1.38.0 (HEAD)
  labels:
    io.kompose.service: postgres-chat
  name: postgres-chat
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: postgres-chat
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -o k8s/
        kompose.version: 1.38.0 (HEAD)
      labels:
        io.kompose.service: postgres-chat
    spec:
      containers:
        - env:
            - name: POSTGRES_DB
              value: glense_chat
            - name: POSTGRES_PASSWORD
              value: changeme
            - name: POSTGRES_USER
              value: glense
          image: postgres:16-alpine
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U glense -d glense_chat
            failureThreshold: 5
            periodSeconds: 10
            timeoutSeconds: 5
          name: glense-postgres-chat
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgres-chat-data
      restartPolicy: Always
      volumes:
        - name: postgres-chat-data
          persistentVolumeClaim:
            claimName: postgres-chat-data
