apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -o k8s/
    kompose.version: 1.38.0 (HEAD)
  labels:
    io.kompose.service: chat-service
  name: chat-service
spec:
  ports:
    - name: "5004"
      port: 5004
      targetPort: 5000
  selector:
    io.kompose.service: chat-service
